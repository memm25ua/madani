---

---

<div class="smooth-wrapper">
    <div class="smooth-content">
        <div class="hero-header">
            <div id="text">
                <div class="line">
                    <p class="word">Madani</p>
                    <p class="word">studio</p>
                </div>

                <div class="line">
                    <p class="word">diseño</p>
                    <p class="word">web</p>
                </div>

                <div class="line">
                    <p class="word">ia</p>
                    <p class="word">software</p>
                </div>

                <div class="line">
                    <a
                        id="hero-link-01"
                        href="mailto:madani.devel@gmail.com"
                        target="_blank"
                        rel="noopener"
                        class="word fancy"
                    >
                        &#x2192;Email
                    </a>

                    <a
                        id="hero-link-02"
                        href="https://www.instagram.com/studiomadani/"
                        target="_blank"
                        rel="noopener"
                        class="word fancy"
                    >
                        &#x2192;Insta
                    </a>
                </div>
            </div>
            <div class="scroll-down-arrow">
                <svg
                    id="scroll-down-arrow"
                    width="24"
                    height="24"
                    viewBox="0 0 24 24"
                    fill="none"
                    xmlns="http://www.w3.org/2000/svg"
                >
                    <path
                        d="M12 5V19M12 19L19 12M12 19L5 12"
                        stroke="white"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"></path>
                </svg>
            </div>
        </div>
    </div>
</div>

<style>
    .hero-header {
        user-select: none;
    }
    .smooth-wrapper {
        min-height: 100vh;
        width: 100%;
    }

    .smooth-content {
        will-change: transform;
    }
    :global(html, body) {
        scroll-behavior: auto;
    }
    .scroll-down-arrow {
        position: absolute;
        bottom: 2rem;
        left: 50%;
        transform: translateX(-50%);
        animation: bounce 2s infinite;
        opacity: 0.7;
    }

    @keyframes bounce {
        0%,
        20%,
        50%,
        80%,
        100% {
            transform: translate(-50%, 0);
        }
        40% {
            transform: translate(-50%, -15px);
        }
        60% {
            transform: translate(-50%, -7px);
        }
    }

    .full-width-hero {
        width: 100vw;
        position: relative;
        left: 50%;
        transform: translateX(-50%);
    }
</style>

<script>
    import Lenis from "lenis";
    import gsap from "gsap";

    if (typeof window !== "undefined") {
        document.addEventListener("DOMContentLoaded", function () {
            // 1. LENIS SMOOTH SCROLL
            const lenis = new Lenis({});

            function raf(time) {
                lenis.raf(time);
                requestAnimationFrame(raf);
            }
            requestAnimationFrame(raf);

            // 2. CURSOR HOVER EVENTS
            const link1 = document.getElementById("hero-link-01");
            const link2 = document.getElementById("hero-link-02");

            const setupHover = (element, message, scale) => {
                element?.addEventListener("mouseenter", () => {
                    window.dispatchEvent(
                        new CustomEvent("cursor-hover", {
                            detail: { active: true, message, scale },
                        }),
                    );
                });
                element?.addEventListener("mouseleave", () => {
                    window.dispatchEvent(
                        new CustomEvent("cursor-hover", {
                            detail: { active: false, scale: 1 },
                        }),
                    );
                });
            };

            setupHover(link1, "Abrir", 1.5);
            setupHover(link2, "Abrir", 1.5);

            const words = document.querySelectorAll(".hero-header .word");
            words.forEach((word) => {
                word.addEventListener("mouseenter", function () {
                    window.dispatchEvent(
                        new CustomEvent("cursor-hover", {
                            detail: {
                                active: true,
                                scale: 4,
                            },
                        }),
                    );
                });

                word.addEventListener("mouseleave", function () {
                    window.dispatchEvent(
                        new CustomEvent("cursor-hover", {
                            detail: { active: false, message: "", scale: 1 },
                        }),
                    );
                });
            });
        });
    }
</script>
